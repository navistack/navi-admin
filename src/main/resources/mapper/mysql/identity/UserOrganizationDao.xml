<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.navistack.admin.modules.identity.dao.UserOrganizationDao">
    <select id="selectAllByQuery" resultType="org.navistack.admin.modules.identity.entity.UserOrganization">
        SELECT *
          FROM user_organization
        <where>
            <if test="id != null">AND id = #{id}</if>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="organizationId != null">AND organization_id = #{organizationId}</if>
        </where>
    </select>

    <select id="existsByQuery" resultType="java.lang.Boolean">
        SELECT EXISTS(
            SELECT *
              FROM user_organization
            <where>
                <if test="id != null">AND id = #{id}</if>
                <if test="userId != null">AND user_id = #{userId}</if>
                <if test="organizationId != null">AND organization_id = #{organizationId}</if>
            </where>
        )
    </select>

    <select id="countByQuery" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM user_organization
        <where>
            <if test="id != null">AND id = #{id}</if>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="organizationId != null">AND organization_id = #{organizationId}</if>
        </where>
    </select>

    <select id="paginateByQuery" resultType="org.navistack.admin.modules.identity.entity.UserOrganization">
        SELECT *
          FROM user_organization
        <where>
            <if test="query.id != null">AND id = #{query.id}</if>
            <if test="query.userId != null">AND user_id = #{query.userId}</if>
            <if test="query.organizationId != null">AND organization_id = #{query.organizationId}</if>
        </where>
        <if test="pageable != null">
            <trim prefix="ORDER BY" suffixOverrides=",">
                <if test="pageable.sort != null and pageable.sort.orders != null and !pageable.sort.orders.isEmpty()">
                    <foreach item="order" collection="pageable.sort.orders" separator=",">
                        <choose>
                            <when test="order.property == 'userId'">user_id</when>
                            <when test="order.property == 'organizationId'">organization_id</when>
                            <otherwise>id</otherwise>
                        </choose>
                        <choose>
                            <when test="order.ascending">ASC</when>
                            <otherwise>DESC</otherwise>
                        </choose>
                    </foreach>
                </if>
            </trim>
            <trim prefix="LIMIT">
                #{pageable.pageSize}
            </trim>
            <trim prefix="OFFSET">
                #{pageable.offset}
            </trim>
        </if>
    </select>

    <select id="selectByQuery" resultType="org.navistack.admin.modules.identity.entity.UserOrganization">
        SELECT *
        FROM user_organization
        <where>
            <if test="id != null">AND id = #{id}</if>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="organizationId != null">AND organization_id = #{organizationId}</if>
        </where>
    </select>

    <select id="selectById" resultType="org.navistack.admin.modules.identity.entity.UserOrganization">
        SELECT *
          FROM user_organization
         WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO user_organization
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="userId != null">user_id,</if>
            <if test="organizationId != null">organization_id,</if>
            <if test="createdAt != null">created_at,</if>
            <if test="createdBy != null">created_by,</if>
        </trim>
        <trim prefix="VALUES(" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="userId != null">#{userId},</if>
            <if test="organizationId != null">#{organizationId},</if>
            <if test="createdAt != null">#{createdAt},</if>
            <if test="createdBy != null">#{createdBy},</if>
        </trim>
    </insert>

    <update id="updateById">
        UPDATE user_organization
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="organizationId != null">organization_id = #{organizationId},</if>
            <if test="updatedAt != null">#{updatedAt},</if>
            <if test="updatedBy != null">#{updatedBy},</if>
        </set>
        <where>
            id = #{id}
        </where>
    </update>

    <delete id="deleteById">
        DELETE
          FROM user_organization
         WHERE id = #{id}
    </delete>

    <delete id="deleteAllByQuery">
        DELETE
          FROM user_organization
        <where>
            <if test="id != null">AND id = #{id}</if>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="organizationId != null">AND organization_id = #{organizationId}</if>
        </where>
    </delete>
</mapper>
